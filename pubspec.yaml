name: flutter_chip8
description: A Chip8 emulator for Flutter
version: 0.0.1
author:
homepage: https://github.com/mjohnsullivan.flutter_chip8

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  chip8:
    path:
      ../chip8
    # git:
    #  url: git://github.com/mjohnsullivan/chip8.git


dev_dependencies:
  flutter_test:
    sdk: flutter